# Quality Score

> Grades each domain and architectural layer. Updated as the project evolves.

## Domain Grades

| Domain | Types | Config | Repo | Service | Overall |
|--------|-------|--------|------|---------|---------|
| data-ingestion | A | A | B | B+ | **B+** (2026-09-07: pipeline tested and verified vs real data, all 3 series; per-race writes now atomic (fetch-all-then-one-transaction, injected-failure tested, backfill continues past a bad race); fallback-results write path + points-race ordinal reads for the nascaR.data adapter) |
| analytics | A | A | B | B | **B+** (2026-07-05: metric math unit-tested, e2e compute tested, output verified vs known history; expectations are league-wide buckets — not yet era/track-type-adjusted) |
| drivers | A | A | B | B | **B+** (2026-07-05: summaries/race log/lookup + cross-series career (unit + e2e) tested; identity verified stable; no headshots/bio metadata) |
| web app (app layer + runtimes) | — | — | — | A- | **A-** (2026-09-07 WS-G: streamed CSV export for every site table (registry-driven; a column/row-width invariant test covers all 14 datasets at once), Excel-compat pinned by tests (BOM bytes, CRLF, quoting, formula-injection guard), lazy-pull and cancel behavior asserted, gzip round-trip verified through the real server, Pro gating proven for *every* registered dataset; deep tools (compare-4 cross-series/season-range, track-explorer full range) and the email layer (pure templates, orchestration with idempotency/suppression/Pro checks, Svix-verified webhook incl. forged/tampered/stale/unconfigured cases). 2026-09-07: e2e route tests incl. series switching + metrics leaderboards, <60ms renders; production pipeline added per WS-B — env validation (fail-fast, negative-tested), request ids, JSON request logs, per-route cache control, CSP/HSTS/nosniff headers, gzip, `/health` incl. 503-on-unreadable-db test, in-process refresh cron with a TTL advisory lock (contention/takeover/failure paths tested); WS-D added per-request viewer resolution, CSRF-checked auth routes, series/feature gating with a teaser (e2e-tested: anon → teaser + 403 JSON, Pro → real page, public-vs-private cache headers). 2026-09-07 WS-I closed `script-src 'unsafe-inline'`: config moved to `<html data-*>`, every `on*=` handler moved to `client/boot.js`, and the guard is behavioral — the real boot.js is evaluated against a stub DOM and each replacement handler driven (confirm/cancel, empty-value navigation, print, worker registration, live-dot success/failure/DOMContentLoaded), plus template and live-response scans that were mutation-checked against the markup they replaced. The static `_headers` is now built from the same `securityHeaders()` as the server and unit-tested. Gaps: not yet deployed (owner accounts pending), `style-src` still allows inline styles (deliberate — per-row computed widths/colors), single-column layout, placeholder wordmark; digest transport unexercised until Resend exists; PWA install + push delivery unverified on real devices) |
| live (Workers-safe domain + edge DO) | A | A | — | B | **B+** (2026-08-07: pure domain fully unit-tested and Workers-portable; deployed edge DO + main-site `/live`. Strategy model calibrated from the backfill and **held-out backtested** — per-track pit-cadence prediction is 60% lower MAE than the flat baseline (6.0 vs 15.0 laps, ±10 laps for 86% of held-out stints); tire severity validated by cross-series face-validity ordering. Weekly refresh now regenerates/deploys both Worker artifacts and refuses an empty calibration; schedule canonicalization guards partially rolled feed identity. Gaps: full-race Phase 4 soak remains, metrics are live-counter estimates (no post-race authoritative swap), DO stops when unwatched, intermediate-track cadence MAE ~10 laps) |
| predictions | A | A | B+ | A- | **A-** (2026-09-07 WS-F: pure deterministic model (seeded PRNG, probability-sum/monotonicity/leakage-guard tests), point-in-time feature reads (the leaky `driver_form` deliberately not reused), DFS scoring config-driven with hand-computed exact tests + malformed-config negatives, e2e page gating tested. **Held-out-2025 backtest passes the spec honesty bar** (beats uniform + trailing-5 on all three Brier events; 6/7 calibration bins inside ±5, 7th within 1 SE). Gaps: entry-list heuristic (regulars only), DK/FD point values unverified vs live platforms, Cup only) |
| accounts | A | A | A- | A- | **A-** (2026-09-07 WS-G added email preferences: opt-in-by-default-off digest lists, per-user unsubscribe tokens with undo, bounce/complaint suppression, and a send ledger whose claim is exclusive while in flight yet retryable after a *recorded* failure — all negative-tested. 2026-09-07 WS-D: argon2id via Bun.password, hash-only token storage, enumeration-safe failures with a timing-equalizing dummy verify, rolling sessions, single-use expiring verify/reset tokens, sliding-window rate limits — all negative-tested (wrong password, reused/expired tokens, limit trips, weak/common passwords) plus e2e over a real server with a cookie jar. 2026-09-07 WS-I added the live HIBP k-anonymity check on sign-up and reset — the provider is negative-tested (non-200, thrown fetch, timeout abandonment, padding entries, case-insensitive match) with an explicit assertion that neither the password nor its hash suffix reaches the wire, and the service side covers rejection, corpus-miss acceptance, fail-open on an unavailable check, offline-list precedence, no lookup for a duplicate email, and a rejected reset leaving the single-use link unburned. Verified against the real API. Gaps: the check fails open by design (an outage lets a breached password through) and never re-checks an existing password; no 2FA (not in v1 spec)) |
| billing (entitlement slice) | A | A | B+ | B+ | **B+** (2026-09-07 WS-D: spec-§7 entitlement model with boundary tests (future/past/exactly-now), manual grants + `bun run grant`, verified-email purchase guard. Deliberately partial: the Stripe webhook state machine, portal, and `stripe_events` idempotency are WS-E) |
| data-health (canary + fallbacks) | A | A | B+ | A- | **A-** (2026-09-07 WS-C: pure runner + validators unit-tested incl. HTTP/shape/transport classification; `feed_status` persistence with streak/outage math and threshold-crossing alerts (one email per outage) fully tested incl. recovery/re-alert; on-page "data delayed" banner e2e-tested; nascaR.data fallback mapping pure + tested and **verified exact against the real release** (3/3 2026 races, 35/36 2025), fills the YellaWood hole. Gaps: real email send untested until Resend exists; SportsDataIO stub schema unverified; alias tables are manual) |
| notifications (Web Push) | A | A | B+ | A- | **A-** (2026-09-07 WS-H: RFC 8291 encryption + RFC 8292 VAPID via WebCrypto, verified by round-tripping through an independent receiver implementation and by verifying the JWT signature against the advertised key; negative-tested (wrong auth secret, tampered ciphertext, malformed subscriber key, bad endpoint, 410 pruning, failure ceiling). Policy is pure and fully tested: kind/driver filtering, timezone-aware quiet hours incl. midnight wrap and fail-open on an unknown zone, dedup that ignores message text. Gaps: never delivered to a real device (no VAPID keys, no deploy), no RFC 8291 published-vector check — the round trip stands in) |
| odds | — | — | — | — | Deferred (no viable odds source) |

## Scoring Criteria

- **A**: Complete, tested, documented, enforced
- **B**: Functional, mostly tested, some gaps
- **C**: Works but has known issues or missing tests
- **D**: Incomplete or has architectural violations
- **F**: Missing or broken
