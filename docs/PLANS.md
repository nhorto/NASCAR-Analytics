# Execution Plans Index

> All execution plans for NASCAR Analytics.

## Active Plans

| Plan | Status | Description |
|------|--------|-------------|
| [Production + Paid Launch](exec-plans/active/2026-09-07-production-and-paid-launch.md) | ACTIVE | Plan (2026-09-07, revised 2026-09-08) from the frozen static site to a production Bun server on Fly.io with accounts, dual-path billing (Stripe on web + RevenueCat IAP in the apps, one server-side entitlement truth), gating (free = Cup + live board; Pro = all series + predictions + DFS + deep tools + push + preview email), an installable PWA **and** a native Expo iOS/Android app (WS-J), data-resilience work (canary, fallback adapters, cold-backfill fix, feed-loss runbook), legal docs, and launch hardening. Web and mobile launch together when both are ready (the 2026-11-01 date is no longer hard). Decisions register inside. Spec: [v1 paid product spec](product-specs/2026-09-07-v1-paid-product-spec.md). Background: [productization review](research/2026-09-07_productization-review.md). |
| [WS-B: Production Server on Fly.io](exec-plans/active/2026-09-07-ws-b-production-server.md) | ACTIVE | Build detail for the launch plan's WS-B: hardened `server.ts` (env validation, request ids, structured logs, per-route cache control, security headers, gzip, `/health`), Dockerfile + fly.toml + Litestream on a volume, in-process weekly-refresh cron (advisory lock, CLI as child process), worker origin from config, restore drill, deploy/backup runbooks. Acceptance boxes live in the launch plan; deploy itself is owner-gated (Fly account, replica bucket, domain). |
| [WS-B2: Railway Migration](exec-plans/active/2026-09-08-ws-b2-railway-migration.md) | ACTIVE | Amends WS-B for the owner's 2026-09-08 decision to host on **Railway** instead of Fly.io (D10 restated). The `Dockerfile` and Litestream entrypoint are provider-neutral and ship as-is; what moves is the config (`fly.toml` → `.railway/railway.ts`), the deploy runbook, and ~20 doc/CLI references. Railway service settings (1 replica, no sleep, `/data` volume, `/health`, port 8080) match what the in-process crons require. Name-bearing values are parameterized to one constant so the pending naming decision is a one-line change, not another repo-wide grep. Owner-gated: Railway payment method, R2 replica bucket. |
| [WS-C: Data Resilience](exec-plans/active/2026-09-07-ws-c-data-resilience.md) | ACTIVE | Build detail for the launch plan's WS-C: `feed_status`-backed canary v1 with owner-email alerts on two consecutive failures + a "data delayed" page notice, per-race ingest transactions, the nascaR.data Parquet fallback adapter (`sync --source nascar-data`), the SportsDataIO not-for-production stub, the loop-metrics-from-lap-timing spike, and the feed-loss runbook. |
| [WS-F: Predictions and DFS](exec-plans/active/2026-09-07-ws-f-predictions-and-dfs.md) | ACTIVE | Build detail for the launch plan's WS-F (pulled forward — WS-E waits on Stripe): `predictions` domain (point-in-time features, rating, seeded Monte Carlo finishing-order simulation, DFS scoring over root-level `config/dfs/*.json`), `bun run predict` + Thursday/Saturday crons, `/predictions` + `/dfs` + methodology pages with the free top-3 teaser, and the honesty-bar backtest (train 2022–24, held-out 2025, Brier vs uniform + trailing-5 baselines). |
| [WS-G: Deep Tools and Email](exec-plans/active/2026-09-07-ws-g-deep-tools-and-email.md) | ACTIVE | Build detail for the launch plan's WS-G: streaming Pro CSV export for every site table (dataset registry, Excel-safe BOM/CRLF, filters in the filename), track-explorer full range control + compare-up-to-four across series and season ranges, and the email layer — verify/reset/Monday-recap/Thursday-preview templates, opt-in preferences, one-tap unsubscribe, Resend bounce/complaint webhook with address suppression, and idempotent digest sends wired into the refresh/predict crons. |
| [WS-H: PWA and Push](exec-plans/active/2026-09-07-ws-h-pwa-and-push.md) | ACTIVE | Build detail for the launch plan's WS-H: installable PWA (generated icons, manifest, privacy-safe service worker that refuses to cache personalized responses, offline page, iOS install guidance) and Web Push (VAPID + RFC 8291 encrypted payloads, Pro subscription flow on the Live page, server-side dispatcher with per-user dedup and quiet hours). |
| [WS-I: Launch Hardening](exec-plans/active/2026-09-07-ws-i-hardening.md) | ACTIVE | Build detail for the launch plan's WS-I (week 8) — the part that needs no owner accounts: closing `script-src 'unsafe-inline'` by removing every inline script and `on*=` handler (page config moves to `<html data-*>`, behavior to `client/boot.js`) and shipping the same security headers from the static export's `_headers`; a live Have I Been Pwned k-anonymity breached-password check on sign-up and reset that fails open; and pinning wrangler exactly so the weekly deploy leg stops fetching an unknown version from the registry. The rest of WS-I (security review, load test, restore re-drill, error alerting, launch checklist) stays owner- and deploy-gated. |
| [WS-J: Native Mobile App](exec-plans/active/2026-09-08-ws-j-native-mobile-app.md) | ACTIVE | Build detail for the launch plan's WS-J (added 2026-09-08, decisions D19–D21): a self-contained `mobile/` Expo SDK 57 + expo-router app (iOS + Android) consuming the existing server — shell + free Cup tier + live board, email/password auth against the existing endpoints, entitlement gating that reads server truth, RevenueCat IAP behind a stubbed purchase interface until owner accounts exist, a RevenueCat webhook as the second entitlement writer beside WS-E's Stripe machine (max-`pro_until` reconciliation, per-channel event dedup), native APNs/FCM push (supersedes Web Push on mobile only), and EAS/store submission. Owner steps J1–J5 tracked in the parent plan. |
| _Other candidates_ | | SEO/OpenGraph metadata + sitemap, cross-series statistical comparison, sharper metric baselines (era/track-type). |

## Completed Plans

| Plan | Completed | Description |
|------|-----------|-------------|
| [Mobile Device Drive](exec-plans/completed/2026-09-09-mobile-device-drive.md) | 2026-09-09 | First run of the app on real devices — iOS simulator **and the project's first Android emulator run** — driven with Maestro in both entitlement states against a 3-series database. Closes WS-J's standing "no screen has ever rendered" gap for the nav surfaces. Found and fixed a **P1**: a successful sign-in was reported as failure and left the app signed out, because the signed-in `/account` page always carries an "Email not verified" form-error and the classifier read that as a rejection (also skipping the `/api/me` fallback) — with verification mail not yet sendable (A4), that was **every** account on Android. Also fixed a P2 (a series with no data reported "Could not reach the server"). Verified: 5-tab bar with Ionicons and no Pro tab, identical navigation free vs Pro, upsell sheet with contextual reason and honest disabled purchase button, Picks free top-3 / Pro DFS board, and real Xfinity + Trucks boards for Pro. Live-tab red dot still unseen (no live race). |
| [App Nav Realignment](exec-plans/completed/2026-09-09-app-nav-realignment.md) | 2026-09-09 | The app half of the UX realignment. Deleted the "Pro" junk-drawer tab; retabbed to Home · Live · Picks · Stats · Account with real Ionicons and a red live-dot badge on the Live tab (foreground-only `/api/live/status` poll). Predictions + DFS became one "Picks" tab with a segmented switch; Stats became a hub (Drivers/Compare/Track types) over the standings + metric boards. Pro is now inline lock states + one shared `UpsellSheet` any locked tap opens. Added **series browsing** on Stats + Drivers (Cup/Xfinity/Trucks pills over the existing `?series=` endpoints — free viewers get locked pills → the sheet, Pro gets real Xfinity/Trucks data); Live-board series deferred (Cup-first by D16). Entitlement stays 100% server-read. Typecheck clean; mobile tests 95 pass (+3), root 869 pass. Simulator drive remains owner-gated (J3/J4) — the app has still never rendered on a device. |
| [Web Responsive Realignment](exec-plans/completed/2026-09-09-web-desktop-breakpoint.md) | 2026-09-09 | The web half of the UX realignment. Below 900px the 8-tab bottom bar became the app's five tabs (Home · Live · Picks · Stats · Account) with inline-SVG icons and a new `/stats` hub page; at 900px+ a grouped desktop sidenav (Race weekend / Results / Stats / Tools) replaces the phone chrome and Home flows into two columns. Predictions + DFS are now a "Picks" surface with a shared segmented control and reach primary navigation for the first time. One HTML for every width; no API/gating/cacheability change. Verified with headless-Chrome screenshots at 390/1280px in both entitlement states (drove real sign-in + `bun run grant`); `bun test` 866 pass, export 630 pages. One caching caveat logged to tech-debt (Pro nav chrome can be served stale from a cached public page). |
| [First-Drive Verification](exec-plans/completed/2026-09-08-first-drive-verification.md) | 2026-09-08 | The first end-to-end *drive* of both surfaces (2026-09-08) — nothing built through WS-J had ever been run and clicked, only unit-tested. Web driven in a real mobile browser (25 routes signed out, auth, free and Pro entitlement states, 12 CSV exports, compare/tracks/search/live interaction, CSRF and enumeration checks); native driven in Expo Go on an iOS Simulator via AXe (all 15 routes, free/locked and Pro states). Six defects found and **all fixed**: native sign-in reported failure on success (P1 — the app stayed signed out despite a valid server session, so Pro could never unlock on mobile), Pro subscribers shown `bun run backfill` shell instructions on Xfinity/Trucks (P1), signup/resend-verify claimed an email was sent when it failed (P2), CSV exports wrote raw unrounded floats (P2), date-only values rendered a day early (P2), no `<h1>` on any page (P3). Xfinity and Trucks were also backfilled 2019–2026 (264 + 187 races), closing the data gap behind the empty state. |
| [Brand Exploration V3](exec-plans/completed/2026-09-08-brand-exploration-v3.md) | 2026-09-08 | Four generated tire/track PNGs from real tire references, self-contained HTML studio with editable sidewall name/tagline, image viewer and downloads, phone previews, and retained vector ideas. Delivered to Downloads; browser checks and all 664 tests pass. |
| [Naming and Logo Exploration](exec-plans/completed/2026-09-08-brand-exploration.md) | 2026-09-08 | Standalone HTML studio: six logo directions including the lettered tire, five name candidates, three palettes, editable names, phone/app/store mockups, small-size checks, and SVG/PNG downloads. Browser-reviewed; 664 tests pass. Brand selection remains open. |
| [Live Race Day Companion](exec-plans/completed/2026-07-05-live-race-companion.md) | 2026-09-08 | Free, in-app, mobile-first live companion: layered live loop-data board (`looplab-arh.pages.dev/live`), proprietary live metrics, strategy/pit-cycle tracking, and driver alerts, served by the `looplab-live` Cloudflare Worker/Durable Object. Phase 4 closed with a full-race soak (`bun run soak`) replaying two archived Cup races end-to-end through the real production composition — zero invariant violations. |
| [WS-D: Accounts and Gating](exec-plans/completed/2026-09-07-ws-d-accounts-and-gating.md) | 2026-09-07 | Launch plan WS-D (week 3, delivered week 1): `accounts` domain (argon2id sign-up/verify/sign-in/reset/delete, hash-only tokens, rolling sessions, CSRF double-submit, sliding-window rate limits, enumeration-safe failures), the `billing` entitlement slice (`pro_until`/`pro_source`, `bun run grant`, verified-email purchase guard), gating (free = Cup + careers; Xfinity/Trucks → Pro teaser incl. un-prefixed race/recap; series JSON → 403), teaser + `/account` v0 + `/pricing` v0, private cache-control for cookie-bearing requests. All acceptance boxes ticked (negative-heavy tests + e2e cookie-jar flows). Stripe checkout/webhooks remain WS-E. |
| [Strategy Model Calibration](exec-plans/completed/2026-07-06-strategy-model-calibration.md) | 2026-07-06 | Replaced the live Strategy tab's fake `DEFAULT_STINT_LAPS=40` + fuel-burn-confounded falloff with a calibrated model from the backfill: per-track **typical green run** + a **pit-discontinuity tire-severity tier** (Darlington→Talladega ordering, cross-series consistent), baked series-keyed into the worker with a track_id→type fallback, plus an honest Strategy UI. **Held-out backtested** (train <2022 / test 2022): per-track pit-cadence prediction is **60% lower MAE than the flat baseline** (6.0 vs 15.0 laps; ±10 laps for 86% of held-out stints). A physical fuel capacity is deliberately unmodeled (not recoverable from history). Deployed to `looplab-live` + Pages. |
| [Real Playoff-Format Model](exec-plans/completed/2026-07-05-playoff-format-model.md) | 2026-07-05 | Replaced the recap's simplified cut line with a season-phase-aware Playoff Picture: regular-season win-and-in field + points cut, and playoff rounds (16→12→8→4 per series) with race-winner auto-advance and eliminations. Phase derived from the last-N races of the ingested schedule. |
| [Weekly Auto-Refresh CI](exec-plans/completed/2026-07-05-weekly-refresh-ci.md) | 2026-07-05 | Portable `bun run refresh` (backfill + compute + export + deploy, all series; env-configurable, deploy self-gates on Cloudflare secrets) scheduled by a GitHub Actions workflow (Mondays 12:00 UTC + dispatch) that caches the DB across runs. Green and artifact-verifiable before Cloudflare is connected; drop-in for a future Cloudflare Container. |
| [Weekly Race Recap](exec-plans/completed/2026-07-05-weekly-recap.md) | 2026-07-05 | Auto-generated post-race recap per series: result summary, per-race proprietary-metric standouts (adjPE + Closer, new `race_metric_standouts` table), points-standings movement with a per-series cut line, and form-vs-result driver callouts. `/recap` + `/recap/{id}` + `/api/recap/:id`; new Recap nav tab; Home CTA repointed. Runs off already-computed data — no live feed. |
| [Cross-Series Career Pages](exec-plans/completed/2026-07-05-cross-series-careers.md) | 2026-07-05 | Un-prefixed `/driver/{id}` career page unifying a driver's Cup+Xfinity+Truck record: grand totals, per-series breakdown, and a season × series timeline matrix. Linked from each series profile; `/api/driver/:id/career`. |
| [Surface the Moat Metrics](exec-plans/completed/2026-07-05-surface-moat-metrics.md) | 2026-07-05 | Made adjPE + Closer Score first-class: a `/metrics` leaderboard page (new nav tab) ranking the season's loop-data regulars, rank/percentile context on driver profiles, and a "Beyond the Box Score" home card. |
| [Fan Analytics Platform MVP](exec-plans/completed/2026-07-05-fan-analytics-mvp.md) | 2026-07-05 | Full MVP: CDN ingestion pipeline + raw archival (Phase 0/1), drivers + analytics domains with proprietary metrics (Phase 2), mobile-first dark web app — profiles, race pages, compare, track explorer (Phase 3). |
| [Multi-Series (Xfinity + Trucks)](exec-plans/completed/2026-07-05-multi-series.md) | 2026-07-05 | Backfilled all 3 national series with loop data; added a Cup/Xfinity/Trucks series switcher threaded through the whole app and JSON API. |
| [Polish Pass + Deployment Prep](exec-plans/completed/2026-07-05-polish-and-deploy-prep.md) | 2026-07-05 | In Form regular filter + track-explorer on-screen filters; researched hosting, chose Cloudflare Pages. |
| [Cloudflare Static Export](exec-plans/completed/2026-07-05-cloudflare-static-export.md) | 2026-07-05 | Path-based series URLs, client-rendered compare/tracks, `bun run export` → dist/ for Cloudflare Pages Direct Upload. See [DEPLOY.md](DEPLOY.md). |

## Tech Debt

See [tech-debt-tracker.md](exec-plans/tech-debt-tracker.md).

## How to Use This

1. **Starting new work?** Check active plans first.
2. **Creating a new plan?** Add to `exec-plans/active/`, update this index.
3. **Finishing a plan?** Move to `exec-plans/completed/`, update this index.
4. **Found tech debt?** Log it in the tech debt tracker.
