# Tech Debt Tracker

> Known technical debt items. Log debt here, don't let it hide.

| Item | Severity | Domain | Description | Logged |
|------|----------|--------|-------------|--------|
| 2025 YellaWood 500 results missing (recovery path now exists) | Low | data-ingestion | CDN weekend-feed serves `weekend_race: null` for race 5580. As of 2026-09-07 (WS-C) `bun run sync --source nascar-data --season 2025` fills it from the nascaR.data release (verified locally; team_id/qualifying/playoff-points columns stay NULL). Run once against the production db after the Fly deploy, then re-`compute`. | 2026-07-05 |
| ~~race_type_id=3 unidentified~~ RESOLVED | — | data-ingestion | Identified 2026-07-05: race 5586, the 2025 Cook Out Clash at Bowman Gray — an exhibition variant. Points filter is `race_type_id = 1`; analytics excludes 2 and 3. | 2026-07-05 |
| weekend_runs not ingested | Low | data-ingestion | Practice/qualifying runs in weekend-feed are archived raw but not normalized into tables. Add if practice-speed analysis becomes a Phase 2 metric. | 2026-07-05 |
| Expectation baselines are league-wide | Low | analytics | adjPE / Closer Score baselines pool all loop-data seasons (2019+) and all track types into one set of position buckets. Era (Next Gen vs Gen 6) and track-type effects aren't separated; small residual bias possible. Refine once the metrics get UI exposure. | 2026-07-05 |
| ~~Recap playoff picture is points-only~~ RESOLVED | — | analytics/app | Resolved 2026-07-05: replaced with a season-phase-aware model (`playoffPicture`) — regular-season win-and-in field + points cut, and playoff rounds (16→12→8→4 per `PLAYOFF_FORMAT_BY_SERIES`) with auto-advance and eliminations. | 2026-07-05 |
| Playoff model has known approximations | Low | analytics | The playoff model omits: waivers / encumbered wins (win-and-in uses a plain ≥1-win + top-30 gate); exact per-round reset point *totals* (the UI shows round points + carried playoff points, which orders the cut correctly but isn't the broadcast "2000+" number); and nuanced tie-breaks (broken by playoff points then name). Also assumes the current 2017+ format and that the full season schedule is ingested (for the last-N phase split). Refine if/when these edge cases matter. | 2026-07-05 |
| Desktop layout is single-column | Low | app | DESIGN.md layout rules call for 1-col mobile → multi-col desktop grids; the current shell is a centered 520px column on every viewport. Fine for the mobile-first MVP; widen when desktop usage matters. | 2026-07-05 |
| ~~Baked Worker artifacts (baselines + track-strategy) go stale on refresh~~ RESOLVED | — | live/worker | Resolved 2026-08-07: `bun run refresh` now regenerates baselines, recalibrates all three series, and deploys the Worker after Pages. CI caches the required weekend-feed archives (with a one-time v2 cold rebuild), and calibration fails safely rather than overwriting the bake when archives are absent. | 2026-07-05 |
| Live DO stops when unwatched; no official post-race swap | Low | live/worker | The `LiveCoordinator` alarm deletes itself after ~15 min with no `/api/live` traffic (cost control), so on restart its alert diff can span the gap (a burst/misattributed alert). Also, live metrics stay as live-counter *estimates* — the DO never swaps to authoritative `loopstats/prod` after the checkered. Add a race-window cron keep-warm + a post-race authoritative swap when moving past tester MVP. | 2026-07-05 |
| ~~Live page not integrated into main site~~ RESOLVED | — | app/live | Resolved 2026-07-05 (Phase 3): `/live` page + permanent Live nav tab + home 🔴 LIVE banner shipped to the main site, reading the Worker cross-origin. | 2026-07-05 |
| Vercel mirror lags Cloudflare on /live | Low | app | The Phase 3 `/live` build deployed to Cloudflare Pages (`looplab-arh.pages.dev`); the Vercel deploy (`looplab-murex.vercel.app`) aborted on upload (transient) and was not retried. Re-run the Vercel deploy flow (see [[nascar-live-deploy]]) to sync, or retire one host. | 2026-07-05 |
| Live drill-down team labels use manufacturer abbrev | Low | live/app | The board shows the manufacturer abbrev (Tyt/Chv/Frd) and colors badges by it; the mockup showed team names/colors (23XI, Hendrick). The live feed doesn't reliably carry team identity, so team badges are deferred. | 2026-07-05 |
| ~~`track-strategy.ts` baked empty until local calibration~~ RESOLVED | — | live/worker | Resolved 2026-07-06: calibrated all 3 series from the backfill + deployed the worker; the bake is now populated (series-keyed, with a track_id→type fallback). Refresh-wiring folded into the "baked Worker artifacts" item above. | 2026-07-06 |
| ~~Historical `pit_reports` adapter unvalidated~~ RESOLVED | — | live/scripts | Resolved 2026-07-06: 256/909 archives carry non-empty `pit_reports` and `pitStopsFromWeekendPitReports()` parses them (107 Cup races, 39K stops). Keys confirmed (`vehicle_number`, `lap_count`, `pit_in_flag_status`, `*_tire_changed`); no `driver_id`, so the tire join uses `results.car_number`. | 2026-07-06 |
| ~~Falloff fit conflates tire wear and fuel burn~~ RESOLVED | — | live | Resolved 2026-07-06: removed the within-stint OLS `fitFalloff` (perfectly collinear tire/fuel within a green run — unfixable by averaging). Replaced with the **pit-discontinuity** method (`tireDropForStop`): worn−fresh lap-time gap across green 4-tire stops, which orders tracks correctly (Darlington 1.89s → Talladega 0.19s) and bakes a `tireTier`. Pit-cadence prediction held-out backtested (`bun run backtest`): 60% lower MAE than the flat baseline. | 2026-07-06 |
| ~~Track explorer has no season-range control~~ RESOLVED | — | app | Resolved 2026-07-05: added on-screen Since-year + Min-starts selects (auto-submit) to the track explorer; segment/sort links preserve them. Full range control (a `to`-year) is still URL-only but the defaults cover the common cases. | 2026-07-05 |
| ~~Form leaders can surface part-timers~~ RESOLVED | — | analytics | Resolved 2026-07-05: `formLeaders` now restricts to season regulars (ran ≥ `FORM_LEADER_MIN_SEASON_SHARE` = 50% of the season's points races), so a part-timer's few strong starts no longer top the board. | 2026-07-05 |
| ~~Canary v0 alerts only via CI failure~~ RESOLVED (v1 built) | — | data-health | Resolved 2026-09-07 (WS-C): `feed_status` persistence, 2-consecutive-failure owner email (exactly once per outage), on-page "data delayed" banner, and an in-process daily cron for the server. `canary.yml` remains the scheduler until the Fly deploy; retire it with the D18 flip. | 2026-09-07 |
| Fallback name aliases are manual | Low | data-health | The nascaR.data join uses hand-maintained track/driver alias tables in `data-health/config.ts` (EchoPark, Coronado, Chicago, Dover, J.H. Nemechek — from the 2026/2025 releases). A future rename shows up as a skipped race in the sync log (safe, not silent) and needs one config line. | 2026-09-07 |
| SportsDataIO stub schema unverified | Low | providers | `providers/sportsdataio.ts` is written against their public docs, never a real payload (needs the owner's free-trial key). `assertUsable` blocks accidental production use. Verify during a real trial before ever promoting. | 2026-09-07 |
| Cache keep-warm is an interim workaround | Low | ci | `cache-keepwarm.yml` exists only because the weekly refresh runs on GitHub Actions with a 7-day cache. Retire it when the refresh moves onto the production server (launch plan D18). | 2026-09-07 |
| CSP allows 'unsafe-inline' scripts | Low | app | The layout's small bootstrap scripts (`__LIVE_API__`, livedot fetch) force `script-src 'unsafe-inline'` in the server's CSP. Replace with CSP hashes (or move to external files) during launch hardening (WS-I security review). | 2026-09-07 |
| CI still deploys; D18 flip deferred | Low | ci | `.github/workflows/weekly-refresh.yml` still runs the deploying refresh on purpose — flipping to `--no-deploy` before the Fly server exists would re-freeze the public site during the playoffs. Flip (and retire keep-warm) once the launch plan's WS-B box "refresh run on the server…" is ticked. Recipe in docs/runbooks/deploy.md. | 2026-09-07 |
| Breached-password check is an offline blocklist | Low | accounts | Spec §6 asks for a breached-password check; v1 is the ~100-entry common-password set in `accounts/config.ts` (plus min-10 + password≠email). Add an HIBP k-anonymity range lookup (I/O → belongs in a provider) during launch hardening if desired. | 2026-09-07 |
| Static export still ships all three series | Med | app | `bun run export` predates gating — the Xfinity/Trucks pages it emits are ungated static files. Correct today (the static site IS the free product) but must become Cup-only ("static Cup fallback", launch plan §4) at launch cutover, or the paid gate can be bypassed via the fallback host. Do it with the D18 flip, not before. | 2026-09-07 |
| Account deletion doesn't cancel Stripe | Low | billing | `POST /auth/delete` revokes the entitlement row but there is no Stripe subscription to cancel yet. WS-E must add the cancel call before real subscriptions exist (marked TODO in src/app/auth.ts). | 2026-09-07 |
| Verify links are GET | Low | accounts | `/verify/{token}` verifies on GET (one tap from email). Email scanners that prefetch links could consume the single-use token before the user clicks; if observed, switch the landing to a confirm-button POST. | 2026-09-07 |
| DK/FD scoring values unverified vs live platforms | Med | predictions | `config/dfs/dk.json`/`fd.json` are written from documented rules, never checked against a live DK/FD contest. The engine is exact-tested against the config; the config itself needs the owner's two recruited DFS players to verify before launch weekend (launch plan WS-F build item). A wrong value mis-projects every driver. | 2026-09-07 |
| Prediction entry lists are a heuristic | Low | predictions | Entrants = drivers in the last 3 completed points races (96.1% coverage on 2025). One-off entries (road-course ringers, part-timers) are missed until they race. Ingest the CDN entry-list/weekend feed pre-race if coverage matters more later; the methodology page discloses the heuristic. | 2026-09-07 |
| /predictions and /dfs lack nav placement | Low | app | The two Pro pages are URL-reachable and cross-linked (methodology, pricing) but have no tab — the 8-tab mobile tabbar is full. Decide placement (tab swap, home card, or appbar) during WS-H polish. | 2026-09-07 |
| In-container wrangler is fetched on demand | Low | infra | The refresh's deploy leg uses `bunx wrangler` (not a pinned dependency); a fresh machine re-downloads it on the first weekly run and a registry hiccup would fail that run's deploy leg (data steps unaffected; next run self-heals). Pin wrangler as a devDependency if it ever flakes. | 2026-09-07 |
| Resend webhook payload shape unverified | Low | app/providers | `/webhooks/resend` is written against Resend's documented event schema (`type`, `data.to[]`, Svix headers) and tested with synthetic payloads — never against a real delivery event. Signature verification is provider-agnostic and safe; only the field extraction could surprise us. Verify with a real bounce during the A4 setup (Resend's dashboard can replay test events). | 2026-09-07 |
| Digest emails are plain text only | Low | app | v1 sends `text/plain` (renders everywhere, no tracking pixel). If open-rate measurement or richer layout matters later, add a multipart/alternative HTML body — templates are pure builders, so it's an additive change. | 2026-09-07 |
| Unsubscribe acts on GET | Low | app | `/unsubscribe/{token}?list=…` flips the list off on GET so the mail-client link is one tap; an aggressive link-prefetching scanner can therefore unsubscribe a user who never clicked. Mitigated by the Undo button on the landing page. If observed, add RFC 8058 `List-Unsubscribe-Post` one-click POST plus a confirm page for plain GETs. | 2026-09-07 |
| Export links use one series per dataset | Low | app | Every export except `compare` is single-series (`?series=N`). A "Cup + Xfinity together" download needs either repeated calls or a multi-series pass in the registry (compare already models it). Fine while free = Cup and Pro users export per-series. | 2026-09-07 |
